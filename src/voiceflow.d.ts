declare global {
    interface Window {
      voiceflow: any; // You can replace `any` with a more specific type if you have detailed documentation for the `voiceflow` object.
    }
  }
  
  export {};